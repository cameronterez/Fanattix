import { Time } from "@angular/common";

export interface FxEvent{
    id: number,
    name: String,
    description: string,
    date: String
    time: String,
    location: String,
    image: String
}